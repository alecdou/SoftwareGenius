<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="softwareGenius.mapper.QuestionDao">
    <resultMap id="questionMap" type="softwareGenius.model.Question">
        <result property="questionId" column="questionId"/>
        <result property="category" column="category"/>
        <result property="problem" column="problem"/>
        <result property="choice1" column="choice1"/>
        <result property="choice2" column="choice2"/>
        <result property="choice3" column="choice3"/>
        <result property="choice4" column="choice4"/>
        <result property="answer" column="answer"/>
        <result property="difficultyLevel" column="difficultyLevel"/>
    </resultMap>

    <insert id="insert" parameterType="softwareGenius.model.Question">
        insert into question (
             category,
             problem,
             choice1,
             choice2,
             choice3,
             choice4,
             answer,
             difficultyLevel
              )
        VALUES (
             #{category},
             #{problem},
             #{choice1},
             #{choice2},
             #{choice3},
             #{choice4},
             #{answer},
             #{difficultyLevel}
          )
    </insert>
    <update id="update" parameterType="softwareGenius.model.Question">
        update question
        set
             category=#{category},
             problem=#{problem},
             choice1=#{choice1},
             choice2=#{choice2},
             choice3=#{choice3},
             choice4=#{choice4},
             answer=#{answer},
             difficultyLevel=#{difficultyLevel}
        where questionId=#{questionId}
    </update>

    <delete id="delete">
        DELETE FROM question
        WHERE questionId = #{questionId}
    </delete>

    <select id="select" parameterType="java.lang.Integer" resultMap="questionMap">
        select questionId,category,problem,choice1,choice2,choice3,choice4,answer,difficultyLevel
        from question
        where questionId = #{questionId}
    </select>

    <select id="selectAll" resultMap="questionMap">
        select questionId,category,problem,choice1,choice2,choice3,choice4,answer,difficultyLevel
        from question
    </select>
</mapper>
